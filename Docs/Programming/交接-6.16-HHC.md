# HHC UI搭建说明
本文档用于给其他组员快速了解当前UI完成情况及约定。具体UI设计见飞书文档。
## 本次修改
完成S01和S02的UI场景搭建，仅布置了背景图和按钮排版，大部分脚本还未实现。S01中编写脚本`MainMenuManager`实现了简单的弹窗打开和关闭，S02实现了消息栏的信息发送。向消息栏发送信息的示例如下：
```text
public MessageScrollBar msgBar;

// 触发事件时添加普通消息
msgBar.AddNewMsg("今日夜市客流提升，收入增加！");
// 警告红色消息
msgBar.AddNewMsg("卫生检查不合格，罚款500！", true);
// 消息栏消息调用
```

主要修改的文件如下：
```text
Assets/_Project/Scripts/UI
Assets/Scenes
```
UI画布中的数值仅作占位，具体显示数值由后续脚本确定。
